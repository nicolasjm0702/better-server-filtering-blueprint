info:
  name: "Better Server Filtering"
  identifier: "betterserverfiltering"
  description: "Replaces the admin dashboard's your/others toggle with an all-servers view, a filter select, and a sort select."
  version: "1.1.1"
  target: "beta-2026-01"
  author: "Nicolas"

admin:
  view: "admin/view.blade.php"

dashboard:
  components: "components"

requests:
  app: "controllers"
  routers:
    client: "routes/client.php"
